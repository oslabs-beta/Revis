version: '3.3'

services:
  nextjs:
    ports:
      - 3000:3000
    build:
      context: .
      dockerfile: Dockerfile
    environment: 
      - NODE_ENV=production
    volumes:
      - .:/app
      - /app/node_modules
      - /app/.next

